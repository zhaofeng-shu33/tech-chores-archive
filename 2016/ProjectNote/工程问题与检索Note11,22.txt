Monte Carlo methods in statistical physics  M.E.J. Newman 
Dev-C++中无法调出Console 解决方案：查看 Project Options, Type changes to Wins32 Console即可。
OpenGL Reference page: www.glprogramming.com
OpenGL检查某点是否被画过的代码：
GLint pixels[4];
glReadPixels(x,y,2,2,GL_COLOR_INDEX,GL_INT,pixels);
if(pixels[0]==255)则(x,y)点被画过

Computer simulation in physics and engineering (Berlin)

