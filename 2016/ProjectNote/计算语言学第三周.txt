正向最大匹配
最短路径方法 词最短的。
基于机械的切分方法：只用词典有没有这个词。
正解率很低：
因为歧义太多，交集型歧义（不同的字串在不同上下文有不同的切法），组合型歧义。混合型歧义。
找歧义字符串
如 正向最大匹配+逆向最小匹配 方法

基于规则的方法：不单有词典，还要有规则库。
切词至少有两个知识库：词典和规则库（按条来写）先找歧义，根据发现的歧义写规则。
统计方法的基础：不用词典，用专门切词的（大规模）语料库和统计模型（HMM),切词同标注，最大熵模型，线性条件随机场。

